node_modules/
dist/
.env
.env.local
*.log
# Anchored to the repo root's runtime file-storage dir only — an
# unanchored "uploads/" here also matches src/modules/uploads/ (the
# receipt-upload feature module), which silently kept that module's
# source files out of every commit.
/uploads/
